name: user_auth
description: Flutter user authentication with back-end handler.
version: 0.0.1
authors: 
- Creator <creative.mohamed.sayed@gmail.com>
homepage: http://github.msayed.net

environment:
  sdk: ">=2.1.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter
  # shared_preferences: 0.5.4+6
  dio: ^2.2.2

dev_dependencies:
  flutter_test:
    sdk: flutter

flutter:
  plugin:
    androidPackage: net.msayed.user_auth
    pluginClass: UserAuthPlugin
    platforms:
      android:
        package: net.msayed.user_auth
        pluginClass: UserAuthPlugin
      ios:
        pluginClass: UserAuthPlugin